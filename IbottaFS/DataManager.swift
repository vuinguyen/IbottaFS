//
//  DataManager.swift
//  IbottaFS
//
//  Created by Vui Nguyen on 10/17/16.
//  Copyright © 2016 Sunfish Empire. All rights reserved.
//

import Foundation

public class DataManager {
    
    /*
    
    // this function reads local JSON file and it takes in a function as a completion handler
    // that may for example, parse that JSON file
    public class func readLocalJSONFile(filename: String, success: ((data: NSData) -> Void)) {
        dispatch_async(dispatch_get_global_queue(DISPATCH_QUEUE_PRIORITY_DEFAULT, 0), {
            
            let filePath = NSBundle.mainBundle().pathForResource(filename, ofType:"json")
            let data = try! NSData(contentsOfFile:filePath!,
                options: NSDataReadingOptions.DataReadingUncached)
            
            // upon successful reading of the JSON file, call the completion handler
            // to do stuff with the JSON data
            success(data: data)
        })
    }
 */
}